# The repository stores text as LF. Without this, core.autocrlf=true on
# Windows checks files out as CRLF, and `npm run lint` then reports a
# prettier error on almost every line, burying the real findings.
* text=auto eol=lf

*.png binary
*.ico binary
*.woff binary
*.woff2 binary
*.mp3 binary
